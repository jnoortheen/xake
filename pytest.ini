[pytest]

addopts =
    --strict
    --pdbcls=IPython.terminal.debugger:Pdb

    -r sxX

    --cov=xake
    --cov-report=html
    --cov-report=term-missing:skip-covered
    --no-cov-on-fail

cache_dir = .cache

markers =
